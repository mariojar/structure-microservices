spring:
  application:
    name: client-traductor
#  profiles:
#    active: dev
#server:
#    port : 8080   
#  cloud:
#    config:
#      uri: http://172.17.0.1:9000
#      failFast: true #questo ancoera non so a che serve, vedo dopo
#      enabled: true #questo ancoera non so a che serve, vedo dopo


 